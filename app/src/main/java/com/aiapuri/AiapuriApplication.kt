package com.aiapuri

import android.app.Application

/**
 * AI-apuri Application entry point.
 * Future tasks will initialize DI, database, and other components here.
 */
class AiapuriApplication : Application() {
    override fun onCreate() {
        super.onCreate()
    }
}
